# ============================================================
# Bradford Bulls — Track Annotation Pipeline (v3)
# Tested with Python 3.10
# ============================================================

# --- Core ML / CV ---
# Caps are tested against host setup. For Colab, the notebook
# (notebooks/colab_pipeline.ipynb) filters these out at install time and
# uses Colab's pre-installed versions instead — no need to relax caps here.
torch>=2.2.0,<2.5.0
torchvision>=0.17.0
ultralytics>=8.3.0           # YOLO11 + BoT-SORT built-in
opencv-python>=4.9.0
numpy>=1.26.0,<2.0.0
Pillow>=10.0.0

# --- Pose estimation (for pipeline.pose_align — ý tưởng 1) ---
# Lazy-imported inside pose_align.py; safe to install. On Colab the notebook
# skips this because mediapipe pulls protobuf<5 which can conflict.
mediapipe>=0.10.0

# --- Tracking & sampling ---
scikit-learn>=1.4.0           # KMeans clustering (auto_calibrate_kit.py)
scikit-image>=0.22.0          # SSIM, image quality metrics
scipy>=1.12.0
imagehash>=4.3.1              # perceptual hashing dedup

# --- Config & validation ---
pydantic>=2.5.0
pydantic-settings>=2.1.0
PyYAML>=6.0
hydra-core>=1.3.0             # config composition

# --- Reviewer UI (Streamlit) ---
streamlit>=1.32.0
streamlit-shortcuts>=0.1.0    # keyboard shortcuts for fast labeling

# --- CLI & UX ---
click>=8.1.0
rich>=13.7.0
tqdm>=4.66.0
loguru>=0.7.0

# --- Data handling ---
pandas>=2.2.0
pyarrow>=15.0.0               # for parquet output

# --- Exporters ---
roboflow>=1.1.0               # optional, for upload
lxml>=5.1.0                   # CVAT XML

# --- Dev / testing (optional) ---
pytest>=8.0.0
pytest-cov>=4.1.0
black>=24.0.0
ruff>=0.3.0
ipykernel>=6.29.0
jupyter>=1.0.0
